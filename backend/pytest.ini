[pytest]
pythonpath = .
testpaths = tests
# Current coverage is ~88% (see CI output) -- 80% leaves headroom for new,
# not-yet-fully-tested code without blocking on every PR, while still
# failing the build if coverage regresses meaningfully.
addopts = --cov=app --cov-report=term-missing --cov-fail-under=80
